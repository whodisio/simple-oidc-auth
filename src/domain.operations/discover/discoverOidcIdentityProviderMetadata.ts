/**
 * use the oidc discovery flow to fetch this identity provider's oidc metadata
 *
 * ref
 * - https://openid.net/specs/openid-connect-discovery-1_0.html
 */
// export const discoverOidcIdentityProviderMetadata = async ({}: {
//   identityProviderDomain: string;
// }): void => {
//   throw new Error('todo');
// };
