# 3.1.repair.test.defects — iteration 1

## test run

```sh
THOROUGH=true npm run test
```

## defects found

### 1. test:lint:cycles fails (BLOCKER)

**what**: `npm run test:lint:cycles` fails due to circular dependency in helpful-errors@1.5.3

**how**: the declapract upgrade introduced dev dependencies that pull in helpful-errors@1.5.3:
- declapract-typescript-ehmpathy@0.47.69
- declastruct@1.9.1
- declastruct-github@1.4.0

**why**: helpful-errors@1.5.3 has an internal circular dependency:
```
helpful-errors/dist/HelpfulError.js -> helpful-errors/dist/withHelpfulError.js
```

dpdm detects cycles in node_modules intentionally to catch these issues.

**fix**: awaited upstream fix. user rejected pnpm overrides workaround.

## status

**BLOCKED**: cannot proceed until upstream dev dependencies upgrade to helpful-errors@1.7.3+

other tests (commits, types, format, lint:biome, lint:deps) pass.
