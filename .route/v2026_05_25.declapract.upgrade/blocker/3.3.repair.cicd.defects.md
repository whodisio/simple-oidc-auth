# blocker: 3.3.repair.cicd.defects

## what blocks

commits are blocked for org `whodisio` at the org level.

```
error: commits blocked for org whodisio (from @all)
```

## what i tried

1. checked local permission status:
   ```sh
   rhx git.commit.uses get
   ```
   result: `left: unlimited`, `push: allowed`, `stage: allowed`

2. attempted commit:
   ```sh
   printf 'fix(practs): bump to latest best\n\n- upgraded declapract packages\n- applied latest practices' | rhx git.commit.set -m @stdin --mode apply --push
   ```
   result: blocked by org-level restriction

## what i need

human to run:

```sh
git.commit.uses allow --org whodisio
```

this grants commit permission for whodisio org repos.
