# blocker: 3.1.repair.test.defects

## what blocks me

`test:lint:cycles` fails due to circular dependency in `helpful-errors@1.5.3`:

```
• Circular Dependencies
  1) node_modules/.pnpm/helpful-errors@1.5.3/node_modules/helpful-errors/dist/HelpfulError.js
     -> node_modules/.pnpm/helpful-errors@1.5.3/node_modules/helpful-errors/dist/withHelpfulError.js
```

this is a transitive dev dependency from:
- declapract-typescript-ehmpathy@0.47.69
- declastruct@1.9.1
- declastruct-github@1.4.0

the cycle is internal to helpful-errors@1.5.3. newer versions (1.7.3+) have no cycle.

## what i tried

1. identified the root cause via `pnpm why helpful-errors` and lockfile analysis
2. proposed pnpm overrides to force helpful-errors@1.7.3
3. **user rejected overrides**: "no overrides dude. just wait for simple-jwt-auth upgrade dude"
4. documented the blocker in hazards artifact
5. verified all other tests pass (commits, types, format, lint:biome, lint:deps)

## what i need

one of:

1. **wait for upstream fix**: when declapract-typescript-ehmpathy, declastruct, declastruct-github upgrade to helpful-errors@1.7.3+, reinstall and continue

2. **permission to use pnpm overrides**: add `"pnpm": { "overrides": { "helpful-errors": "1.7.3" } }` to package.json

3. **skip the cycle check**: modify `test:lint` to not run `test:lint:cycles` (not recommended)

user chose option 1. this route is blocked until upstream packages are updated.
