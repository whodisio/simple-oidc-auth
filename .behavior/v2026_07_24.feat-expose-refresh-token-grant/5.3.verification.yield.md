# 5.3.verification — buttonup proof for getTokensFromOidcRefreshClaims

verification gate for the wish (`0.wish.md`) + criteria (`2.1.criteria.blackbox.yield.md`). every
test suite run with cited command + exit code + count. zero deferrals, zero skips introduced, zero
unproven claims.

## verification checklist

### behavior coverage (criteria usecases → test citation)

each blackbox usecase from `2.1.criteria.blackbox.yield.md` is proven at unit + integration +
acceptance tiers, with pinned snapshots at the blackbox (acceptance) tier.

| usecase (from criteria) | integration citation | acceptance citation | snapshots? | status |
|--------------------------|----------------------|---------------------|------------|--------|
| 1 — silent refresh, no rotation | `[usecase1][t0]` | `[t1]` | ✓ | ✅ |
| 2 — silent refresh with rotation (single-use) | `[usecase2][t0]` | `[t2]→[t3]` | ✓ | ✅ |
| 3 — expired/revoked refresh (invalid_grant → Expired) | `[usecase3][t0]` | `[t3]`, `[t9]` | ✓ | ✅ |
| 4 — rejected request, scope exceeds grant (invalid_scope → Rejected) | `[usecase4][t0]` | `[t7]` | ✓ | ✅ |
| 5 — rejected request, bad creds (invalid_client → Rejected) | `[usecase5][t0]` | `[t13]` | ✓ | ✅ |
| 6 — endpoint malfunction (503 / 429+Retry-After / socket → Malfunction) | `[usecase6][t0]` | `[t5]`, `[t8]`, `[t12]` | ✓ | ✅ |
| 7 — expiry is an absolute IsoTimeStamp (+ null when omitted) | `[usecase1]` (time-tolerance), `asExpiresAt.test` | `[t1]`, `[t4]` | ✓ | ✅ |
| 8 — reduced scope / resource claim honored | `[usecase8][t0]` | `[t11]` | ✓ | ✅ |
| non-idempotency rotation-collision (Malfunction → retry → Expired) | `[usecase9]` | `[t14]` | ✓ | ✅ |
| boundary — refresh_token echoed == input surfaces non-null | `[usecase2][t1]` | (value-agnostic path) | n/a | ✅ |

no promised behavior is left untested. the three error families are each discriminated by thrown
type at all three tiers (the pit-of-success guarantee of `2.1.criteria`).

### zero skips verified

- [x] no `.skip()` / `.only()` in ANY new refresh-grant file (the local RFC-6749 harness was built
      precisely to `[→] replace` the `.skip`-ped real-provider pattern per the blueprint)
- [x] no silent credential bypass (`if (!creds) return`) anywhere in the new code or tests
- [x] no prior failure carried forward

**the 3 skips that remain** are ALL prior, on UNTOUCHED files, out of this wish's scope:
- `exchangeOidcResponseCodeForTokens.integration.test.ts:17` — real apple code-grant (code-grant leg)
- `getAuthedClaimsFromOidcIdentityToken.integration.test.ts:10` — real google token decode
- `getAuthedClaimsFromOidcIdentityToken.integration.test.ts:20` — real apple token decode

each is a real-provider manual test that needs live Google/Apple OAuth credentials + a real browser
login — physically impossible in CI without foreman-held creds, and a live-network dependency that
`rule.require.hermetic-tests` forbids in CI. they are the repo's prior convention, not a regression
this PR introduced, and not on any file this PR touches. removal is out of scope + would violate
hermetic-tests; they are recorded in the follow-on backlog (`5.1.execution.yield.md` item #1 covers
the code-grant leg's hardening, which is the natural home for its real-provider proof).

### snapshot coverage for the sdk contract

the sdk export IS the contract (no cli/api stream), so the acceptance journey snapshots the public
return object + each thrown error against the BUILT package (blackbox via `link:.`).

| contract | output variants snapped | snapshot file | status |
|----------|-------------------------|---------------|--------|
| `getTokensFromOidcRefreshClaims` (sdk) | success (no-rotation, rotation, omitted-expiry, scoped), + all 3 error families (Expired, Rejected, Malfunction incl. 429+Retry-After + socket-drop), + rotation-collision arc | `response/__snapshots__/getTokensFromOidcRefreshClaims.acceptance.test.ts.snap` | ✅ |
| wire request (mechanism) | grant_type / refresh_token / client_id / client_secret / scope? / resource? | `exchange/__snapshots__/exchangeRefreshTokenForTokens.integration.test.ts.snap` | ✅ |

- [x] every sdk output variant exercised (success + every error family)
- [x] snapshots demonstrate actual consumer-visible output, not just "it ran"

### snapshot change rationalization

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| `response/__snapshots__/getTokensFromOidcRefreshClaims.acceptance.test.ts.snap` | added (new) | yes | first acceptance snapshot for the new sdk contract; captures success + all error families + the `[t14]` rotation-collision arc |
| `exchange/__snapshots__/exchangeRefreshTokenForTokens.integration.test.ts.snap` | added (new) | yes | wire-request mechanism snapshot for the new communicator, at the real-endpoint integration tier |

no prior snapshot was modified — every `.snap` in the diff is net-new for this feature. so there is
no accidental drift on any extant contract to reconcile.

### tests executed — with proof

all commands run 2026-07-27; every exit code observed directly (not assumed).

| suite | command | result | proof |
|-------|---------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, no type errors |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, no format diffs |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0 (biome + cycles + deps) |
| unit | `rhx git.repo.test --what unit --mode apply` | ✅ | exit 0 — 79 passed, 0 failed, 0 skipped (18 suites) |
| integration | `rhx git.repo.test --what integration --mode apply` | ✅ | exit 0 — 48 passed, 0 failed, 3 skipped (prior real-provider, above) |
| acceptance | `rhx git.repo.test --what acceptance --against local --env test --mode apply` | ✅ | exit 0 — 18 passed, 0 failed, 0 skipped (1 suite, blackbox via built package) |

- [x] every test command was run + its output observed
- [x] every exit code verified (0 = pass)
- [x] no test skipped, mocked, or faked in the new deliverable (integration + acceptance hit the
      REAL local RFC-6749 endpoint per `rule.forbid.integration.mocks` / `rule.forbid.acceptance.mocks`)

### contract output snapshot exhaustiveness

| contract type | contract | positive snapped | negative snapped | edge snapped |
|---------------|----------|------------------|------------------|--------------|
| sdk | `getTokensFromOidcRefreshClaims` | ✓ (no-rotation, rotation, scoped) | ✓ (Expired, Rejected, Malfunction) | ✓ (omitted-expiry → null, 429+Retry-After, socket-drop, rotation-collision) |

- [x] positive path snapped (multiple success variants)
- [x] every error family snapped with pinned message + metadata
- [x] edge cases snapped (null expiry, rate-limit backoff, transport drop, single-use collision)

### blockers

none. 5.1.execution passed peer review at i024 (all 9 l1 terminal 0/0; l3 r10 approved 0/0; l3 r11
approved 0 blockers / 1 nitpick under the allow-3 threshold after an l3 budget extension let it
converge past a provider-quota malfunction). all six verification suites green with cited proof above.
